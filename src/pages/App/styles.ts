import { CSSProperties } from "react";

const styles = {
  wrapper: {
    width: "100%",
  } as CSSProperties,
  header: {
    width: "100%",
    justifyContent: "center",
  } as CSSProperties,
  spinner: {
    background: "white",
    width: "100%",
    padding: "16px",
    justifyContent: "center",
  } as CSSProperties,
};

export default styles;
